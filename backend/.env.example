# Copy to .env for local (non-Docker) runs. docker compose sets these directly
# in compose.yaml instead -- this file exists so nothing is ever hardcoded.
DATABASE_URL=postgresql+psycopg2://campusconnect:campusconnect_dev_password@localhost:5432/campusconnect
REDIS_URL=redis://localhost:6379/0
JWT_SECRET=dev-secret-change-me
CORS_ORIGINS=http://localhost:3000
