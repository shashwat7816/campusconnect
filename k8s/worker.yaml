apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
        - name: worker
          image: acrcampusconnect5300d4.azurecr.io/worker:latest

          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: campusconnect-secrets
                  key: database-url

            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: campusconnect-secrets
                  key: redis-url

            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: campusconnect-secrets
                  key: jwt-secret

          resources:
            requests:
              cpu: "50m"
              memory: "96Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"